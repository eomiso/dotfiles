# This is Git's per-user configuration file.
[user]
	name = Uiseop Eom
	email = ytrtef@gmail.com
# Please adapt and uncomment the following lines:
#	name = Uiseop Eom
#	email = ytrtef@gmail.com
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
